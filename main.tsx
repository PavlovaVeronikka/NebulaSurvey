import React from "react";
import ReactDOM from "react-dom/client";
import NebulaSurvey from "./app/nebulaSurvey";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <NebulaSurvey />
  </React.StrictMode>
);
